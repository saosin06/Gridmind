# Generated by the GridMind routing agent
# Workload "training-50mw-12h" routed to San Jose (us-west-1) — run immediately.
# Projected: $-2166.9 energy, 47.43 t CO2.
# Saves $50363.1 and 303.27 t vs Ashburn.
apiVersion: batch/v1
kind: Job
metadata:
  name: training-50mw-12h
  labels:
    app.kubernetes.io/managed-by: gridmind
  annotations:
    gridmind.io/region: "San Jose"
    gridmind.io/cloud-region: "us-west-1"
    gridmind.io/profile: "training"
    gridmind.io/schedule: "now"
    gridmind.io/projected-cost-usd: "-2166.9"
    gridmind.io/projected-co2-tonnes: "47.43"
    gridmind.io/grid-carbon-gco2-kwh: "200"
spec:
  backoffLimit: 2
  template:
    metadata:
      labels:
        app.kubernetes.io/managed-by: gridmind
    spec:
      restartPolicy: Never
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: topology.kubernetes.io/region
                    operator: In
                    values: ["us-west-1"]
      containers:
        - name: workload
          image: ghcr.io/your-org/training-50mw-12h:latest
          resources:
            requests:
              cpu: "50"   # ~1 vCPU per MW (placeholder)
