# Generated by the GridMind routing agent
# Workload "demo-train" routed to San Jose (us-west-1) — run immediately.
# Projected: $-9635 energy, 44.6 t CO2.
# Saves $36829 and 18.2 t vs Ashburn.
apiVersion: batch/v1
kind: Job
metadata:
  name: demo-train
  labels:
    app.kubernetes.io/managed-by: gridmind
  annotations:
    gridmind.io/region: "San Jose"
    gridmind.io/cloud-region: "us-west-1"
    gridmind.io/profile: "training"
    gridmind.io/schedule: "now"
    gridmind.io/projected-cost-usd: "-9635"
    gridmind.io/projected-co2-tonnes: "44.6"
    gridmind.io/grid-carbon-gco2-kwh: "200"
spec:
  backoffLimit: 2
  template:
    metadata:
      labels:
        app.kubernetes.io/managed-by: gridmind
    spec:
      restartPolicy: Never
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: topology.kubernetes.io/region
                    operator: In
                    values: ["us-west-1"]
      containers:
        - name: workload
          image: ghcr.io/your-org/demo-train:latest
          resources:
            requests:
              cpu: "50"   # ~1 vCPU per MW (placeholder)
