# Generated by the GridMind routing agent
# Workload "inference-95mw-12h" routed to Ashburn (us-east-1) — run immediately.
# Projected: $34137.53 energy, 747.06 t CO2.
# Saves $-12580.13 and -506.75 t vs San Jose.
apiVersion: batch/v1
kind: Job
metadata:
  name: inference-95mw-12h
  labels:
    app.kubernetes.io/managed-by: gridmind
  annotations:
    gridmind.io/region: "Ashburn"
    gridmind.io/cloud-region: "us-east-1"
    gridmind.io/profile: "inference"
    gridmind.io/schedule: "now"
    gridmind.io/projected-cost-usd: "34137.53"
    gridmind.io/projected-co2-tonnes: "747.06"
    gridmind.io/grid-carbon-gco2-kwh: "350"
spec:
  backoffLimit: 2
  template:
    metadata:
      labels:
        app.kubernetes.io/managed-by: gridmind
    spec:
      restartPolicy: Never
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: topology.kubernetes.io/region
                    operator: In
                    values: ["us-east-1"]
      containers:
        - name: workload
          image: ghcr.io/your-org/inference-95mw-12h:latest
          resources:
            requests:
              cpu: "95"   # ~1 vCPU per MW (placeholder)
